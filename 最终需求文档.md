## Plan: OLED 三级联动嵌入式项目需求规格

基于基础需求，经与用户多轮确认后梳理的详细可执行需求文档。三层架构：PC ↔ RK3506 ↔ STM32F407 ↔ OLED。

---

## 一、项目概述

### 1.1 项目目标
搭建一个三级嵌入式系统联动项目，实现 PC → RK3506 → STM32F407 → OLED 的全链路数据通信与显示控制，作为嵌入式系统设计的综合练习。

### 1.2 系统拓扑

```
┌──────────┐     TCP(Web/TCP)     ┌────────────┐     UART(自定义帧)    ┌───────────┐   I²C   ┌─────────┐
│  PC 浏览器 │ ◄──────────────────► │  RK3506     │ ◄──────────────────► │ STM32F407  │ ◄──────► │  OLED   │
│ / TCP上位机│   HTTP+WebSocket    │  Linux      │   115200/8N1         │ 裸机 HAL   │         │ 0.96"   │
└──────────┘                      └────────────┘                      └───────────┘         │ 128×64  │
                                                                                            └─────────┘
```

---

## 二、RK3506 端需求（Linux 主控网关）

### 2.1 操作系统与开发环境
- **OS**: Linux (Buildroot 构建，最小化系统)
- **开发语言**: 纯 C (C11 标准)，无 C++ 运行时依赖
- **编译器**: `arm-linux-gcc` (Buildroot 交叉工具链)
- **第三方库**: mongoose 单头文件库 (HTTP + WebSocket)
- **编译选项**: `-DMG_ENABLE_HTTP_WEBSOCKET=1 -D_GNU_SOURCE`
- **IP 地址**: DHCP 获取，同时在 OLED 和 Web 页面上显示

### 2.2 Web 服务器（主要交互通道）
| 项目 | 规格 |
|------|------|
| HTTP Server | 轻量级，推荐 libmicrohttpd 或 mongoose |
| WebSocket | 与前端实时双向通信 |
| 监听端口 | HTTP: 80, WebSocket: 同端口升级 |
| 静态资源 | 内嵌 HTML/CSS/JS 单页应用 |

### 2.3 TCP 服务器（备选调试通道）
| 项目 | 规格 |
|------|------|
| 类型 | 原始 TCP Socket 服务器 |
| 监听端口 | 9527 |
| 功能 | 与 Web 服务器共享同一后端逻辑，接收/发送 JSON 指令 |

### 2.4 串口通信（与 STM32 交互）
| 项目 | 规格 |
|------|------|
| 接口 | UART (TTL 直连，无需 RS232 电平转换) |
| 波特率 | 115200 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 (8N1) |
| 流控 | 无 |

### 2.5 核心功能
1. 接收 Web/TCP 客户端指令，解析后通过串口下发至 STM32
2. 接收 STM32 上传状态（LED 状态、当前显示模式、按键事件、OLED 显存同步），推送至 Web 前端
3. 本地维护 RTC 时间，定期（1s 间隔）同步至 STM32
   - 首次上电 RTC 默认为 2025-01-01 00:00:00，可通过 Web 时间同步指令修改
   - 系统时间同时显示在 Web 前端顶部状态栏
4. 模拟天气数据：提供晴天/多云/阴/小雨/大雨/雷雨/雪 等预设场景，可通过 Web 切换
5. OLED 显存同步：接收 STM32 分片上传的 OLED 显存数据（1024 字节），重组后 Base64 编码推送 Web 前端 Canvas 实时预览
6. 串口测试命令：Web/TCP 端可下发 `serial_test` 指令，在 RK3506 控制台输出测试信息并同时转发至 STM32 串口
7. 串口数据帧收发日志（可选，调试用）

### 2.6 不上网声明
- 本项目 **不连接互联网**
- 天气数据由 RK3506 本地模拟生成
- 时间来源于 RK3506 本地 RTC（首次启动手动设置或默认值）

---

## 三、STM32F407 端需求（执行控制）

### 3.1 固件框架
- **框架**: 裸机开发，使用 STM32 HAL 库
- **开发工具**: STM32CubeMX 生成初始化代码 + Keil / STM32CubeIDE 开发

### 3.2 外设配置
| 外设 | 用途 | 参数 |
|------|------|------|
| USART1 | 与 RK3506 通信 | 115200/8N1, 接收中断 + DMA, 环形缓冲区 |
| USART2 | 调试串口（独立） | 115200/8N1, 仅 TX, 通过 `DEBUG_UART_ENABLE` 宏控制开关 |
| I²Cx | 驱动 OLED 屏 | 标准模式 100kHz, 软件或硬件 I²C |
| GPIO (LED) | 板载 LED 控制 | 推挽输出，支持 亮/灭/闪烁(可调频率) |
| GPIO (KEY) | 物理按键 | 上拉输入，外部中断触发，至少 2 个按键 |
| IWDG | 独立看门狗 | 喂狗周期约 1s, 调试时通过编译宏 `IWDG_ENABLE` 控制开关 |

### 3.3 看门狗策略
- 使用 STM32 独立看门狗 (IWDG)，防止程序跑飞
- **调试模式**: 通过全局宏定义 `IWDG_ENABLE` 控制
  - 定义 `IWDG_ENABLE=1` 时使能看门狗
  - 定义 `IWDG_ENABLE=0` 或未定义时禁止初始化 IWDG（方便调试, 避免断点触发复位）
- 喂狗位置: 主循环 while(1) 末尾，确保主循环正常运转
- 喂狗间隔: ≤ 500ms（IWDG 复位周期设为约 1s，留 50% 余量）
- 看门狗超时触发系统复位，复位后重新加载上电默认显示

### 3.4 自定义二进制帧协议

**帧格式 (RK3506 ↔ STM32F407)**:

```
┌──────┬──────┬──────┬──────┬────────────┬──────┬──────┐
│ SOF  │ LEN  │ CMD  │ SEQ  │   DATA     │ CRC8 │ EOF  │
│ 1B   │ 1B   │ 1B   │ 1B   │  0~251B    │ 1B   │ 1B   │
└──────┴──────┴──────┴──────┴────────────┴──────┴──────┘
```

| 字段 | 大小 | 说明 |
|------|------|------|
| SOF | 1B | 帧头，固定 `0xA5` |
| LEN | 1B | DATA 段长度 (0~251) |
| CMD | 1B | 命令码（见下文） |
| SEQ | 1B | 包序号，递增，ACK 帧回传相同序号 |
| DATA | 0~251B | 负载数据 |
| CRC8 | 1B | CRC-8-ATM 校验（覆盖 SOF~DATA 全长） |
| EOF | 1B | 帧尾，固定 `0x5A` |

**命令码定义**:

| CMD | 方向 | 功能 | DATA 段 |
|-----|------|------|---------|
| `0x01` | PC→STM | LED 控制 | 1B: 0=关, 1=开, 2=闪烁 |
| `0x02` | PC→STM | 设置显示模式 | 1B: 0=静态, 1=左滚, 2=右滚, 3=上滚, 4=下滚, 5=翻页, 6=淡入淡出 |
| `0x03` | PC→STM | 下发文字内容 | 变长: UTF-8 编码文字 (≤200B) |
| `0x04` | PC→STM | 时间同步 | 7B: 原始值编码 [年-2000 1B][月 1B][日 1B][时 1B][分 1B][秒 1B][星期 1B] |
| `0x05` | PC→STM | 天气数据下发 | 4B: [天气类型 1B][温度(有符号) 1B(-20~50℃)][湿度 1B(0~100%)][风向 1B] |

**天气类型编码**:
| 值 | 0 | 1 | 2 | 3 | 4 | 5 | 6 |
|----|---|---|---|---|---|---|---|
| 天气 | 晴 | 多云 | 阴 | 小雨 | 大雨 | 雷雨 | 雪 |

**风向编码**: 0=北, 1=东北, 2=东, 3=东南, 4=南, 5=西南, 6=西, 7=西北
| `0x06` | PC→STM | 设置上电默认文字 | 变长: UTF-8 编码文字 (≤200B), 写入 Flash 保存 |
| `0x07` | — | **[预留] OTA 固件升级** | 留空, 后续扩展 |
| `0x10` | STM→PC | LED 状态上报 | 1B: 当前 LED 状态(同 0x01) |
| `0x11` | STM→PC | 显示模式上报 | 1B: 当前显示模式(同 0x02) |
| `0x12` | STM→PC | 按键事件上报 | 2B: [键号 1B][动作 1B: 0=短按, 1=长按, 2=释放] |
| `0x20` | STM→PC | OLED 显存同步帧 | 2+N: [分片序号 1B][总分片数 1B][显存数据 N 字节] |
| `0xF0` | 双向 | ACK 应答 | 0B: LEN=0 表示成功 |
| `0xFF` | 双向 | NAK/错误 | 1B: 错误码 |

**错误码定义**:
| 错误码 | 含义 |
|--------|------|
| `0x01` | CRC 校验失败 |
| `0x02` | 未知命令码 |
| `0x03` | 参数错误/越界 |
| `0x04` | Flash 写入失败 |
| `0x05` | 系统忙（上一命令未完成） |

**通信规则**:
- 上位机（RK3506）为主设备，主动发起指令
- STM32 收到指令后必须回复 ACK/NAK
- STM32 主动上报按键事件和显存同步帧，RK3506 回复 ACK
- 超时 500ms 未收到 ACK 则重发，最多重发 3 次
- 接收端 CRC 校验失败回复 NAK

**OLED 显存同步帧 (CMD_FRAME_SYNC, 0x20)**:

为实现 Web 前端的 OLED 实时预览，STM32 通过分片方式将 1024 字节的 OLED 显存上传至 RK3506：
- DATA 段格式: `[分片序号 1B][总分片数 1B][显存数据片段 N 字节]`
- 最大分片数: 8 片 (OLED_SEG_MAX)
- 除最后一分外，每片大小均等（1024/total 字节）
- RK3506 收到全部分片后，组装为完整 1024 字节显存，Base64 编码后通过 WebSocket 推送至 Web 前端
- Web 前端解码后使用 Canvas 绘制 128×64 像素 OLED 预览画面

### 3.5 OLED 显示管理

#### 显示区域划分（128×64 多区域组合布局）
```
┌──────────────────────────┐
│  ⏰ 14:30   ☀️ 晴 25°C   │ ← 顶部状态栏 (高 16px)
│          2025-07-29      │   时间 + 天气图标 + 温度
├──────────────────────────┤
│                          │
│    你好，嵌入式世界！      │ ← 主内容区 (高 48px)
│    这是滚动文字演示       │   显示用户输入文字
│                          │   支持所有显示特效
└──────────────────────────┘
```
- **顶部状态栏** (0-15行): 始终显示时间、天气、LED 状态图标
- **主内容区** (16-63行): 显示用户下发的文字，受显示模式控制

#### 显示特效实现
| 模式 | 描述 | 实现方式 |
|------|------|----------|
| 静态显示 | 文字固定在主内容区 | 直接写显存 |
| 向左滚动 | 文字从右向左平滑移动 | 定时器移动显存列偏移 |
| 向右滚动 | 文字从左向右平滑移动 | 同上，反向 |
| 向上滚动 | 文字从下向上平滑移动 | 定时器移动显存页偏移 |
| 向下滚动 | 文字从上向下平滑移动 | 同上，反向 |
| 翻页显示 | 超过一屏内容自动翻页 | 定时器触发的页切换（带过渡） |
| 淡入淡出 | 文字逐渐出现/消失 | 软件 PWM 调节对比度或逐帧刷新 |

#### 中文字库
- 采用取模方式：将常用中文字符（约 500+ 常用汉字）生成 16×16 点阵字模
- 字模数据存放在 STM32 Flash（片上 1MB 足够存放）
- 使用 PC 端 Python 工具 `tools/gen_font.py` 自动生成字模，直接覆盖 `src/font.c` 中的 `chinese_font_table`
- 工具依赖 Pillow 库（`pip install pillow`），自动查找系统可用的中文字体
- 显示时根据汉字内码计算偏移量读取字模

#### 调试串口模块
- **独立调试串口**: 使用独立的 USART（如 USART2）仅 TX 输出调试信息
- **与通信串口隔离**: 调试串口不影响与 RK3506 的主通信链路
- **编译开关**: 通过 `DEBUG_UART_ENABLE` 宏整体控制（0=关闭，1=开启）
- **关闭时零开销**: 关闭时所有 `DEBUG_PRINTF` / `DEBUG_HEXDUMP` / `DEBUG_WRITE` 宏编译为空
- **功能**: 支持 `debug_printf` 格式化输出（自动追加 `\r\n`）、`debug_hexdump` 十六进制 dump
- **信号安全**: 使用 HAL 阻塞发送模式，可安全在中断上下文中调用

#### 上电默认显示
- 上电后 OLED 显示预设的默认欢迎文字（出厂默认: "欢迎进入系统"）
- 默认文字存储在 STM32 Flash 的配置区域（而非硬编码），支持断电保存
- 可通过 Web/TCP 下发 `0x06` 命令修改默认文字，修改后立即生效并写入 Flash
- 上电后自动进入默认文字显示，显示模式为静态，随后可通过按键或指令切换

### 3.6 按键功能
| 按键 | 功能 |
|------|------|
| KEY1 | 切换显示模式（循环：静态→左滚→右滚→上滚→下滚→翻页→淡入淡出→静态） |
| KEY2 | LED 开关/闪烁切换（循环：关→开→闪烁→关） |
| 组合键 | 预留：KEY1 长按 2s 进入配置模式 |

---

## 四、PC 端需求（Web 前端）

### 4.1 技术栈
- HTML5 + CSS3 + 原生 JavaScript（无框架依赖，轻量单页应用）
- WebSocket 与 RK3506 实时通信
- Canvas 实现 OLED 屏幕实时预览

### 4.2 页面布局
```
┌─────────────────────────────────────────────┐
│  OLED 三级联动控制系统            [已连接 ✓] │ ← 顶部标题栏
├──────────┬──────────────────────────────────┤
│ 连接管理  │                                  │
│ ──────── │       ┌──────────────────┐       │
│ IP:       │       │  OLED 实时预览    │       │
│ 192.168..│       │                  │       │
│ 端口:8080 │       │ (128×64 Canvas  ) │       │
│ [连接]    │       │  4x 放大绘制     │       │
│ [断开]    │       │  实时同步显存    │       │
│           │       │                  │       │
├──────────┤       └──────────────────┘       │
│ LED 控制  │                                  │
│ ──────── │  LED 状态: ● 开启                │
│ [开][关] │                                   │
│ [闪烁]   │                                   │
├──────────┤                                   │
│ 显示模式  │                                  │
│ ──────── │                                   │
│ ○ 静态   │                                   │
│ ○ 左滚   │                                   │
│ ○ 右滚   │                                   │
│ ○ 上滚   │                                   │
│ ○ 下滚   │                                   │
│ ○ 翻页   │                                   │
│ ○ 淡入淡出│                                  │
├──────────┤                                   │
│ 文字输入  │                                  │
│ ──────── │                                   │
│ [________]│                                  │
│ [发送]   │                                   │
├──────────┤                                   │
│ 上电默认  │                                  │
│ 文字     │                                   │
│ ──────── │                                   │
│ [________]│                                  │
│ [保存]   │                                   │
├──────────┤                                   │
│ 天气模拟  │                                  │
│ ──────── │                                   │
│ 类型: [▼]│                                  │
│ 温度: [_]│                                  │
│ 湿度: [_]│                                  │
│ 风向: [▼]│                                  │
│ [更新]   │                                   │
├──────────┤                                   │
│ 串口测试  │                                  │
│ ──────── │                                   │
│ [________]│                                  │
│ [发送]   │                                   │
├──────────┤                                   │
│ 按键事件  │                                  │
│ ──────── │                                   │
│ 日志...   │                                  │
└──────────┴──────────────────────────────────┘
```

### 4.3 功能清单
1. **连接管理**: 输入 RK3506 IP 地址及端口，连接/断开 WebSocket
2. **连接状态指示**: 实时显示连接状态（已连接/断开/异常）
3. **LED 控制**: 三个按钮（开/关/闪烁），点击即下发指令至 STM32；LED 状态图标实时反馈
4. **显示模式**: 7 种模式单选框，点击切换 OLED 显示效果；模式选中态实时同步
5. **文字输入**: 文本输入框 + 发送按钮，文字下发至 OLED 显示
6. **上电默认文字**: 设置上电默认显示文字，输入框 + 保存按钮，保存至 STM32 Flash
7. **天气模拟**: 下拉选择天气类型 + 温度/湿度/风向输入，点击更新下发天气数据
8. **按键事件日志**: 实时显示 STM32 上报的按键事件（含时间戳，最多保留 50 条）
9. **OLED 实时预览**: Canvas 4x 放大绘制 128×64 画面，收到 `frame_sync` 事件后解码 Base64 显存数据逐像素绘制；未连接时自动显示演示画面（含时间和欢迎文字）
10. **串口测试**: 输入测试内容后点击发送，RK3506 控制台输出测试日志并同时经串口转发至 STM32

### 4.4 TCP 上位机（备选）
- 简单命令行或轻量 GUI，发送 JSON 指令
- JSON 协议格式：`{"cmd": "led", "data": {"state": 1}}`
- 与 Web 后端共用解析逻辑

---

## 五、通信协议汇总

### 5.1 链路一览
| 链路 | 协议 | 物理层 |
|------|------|--------|
| PC ↔ RK3506 (Web) | HTTP + WebSocket (JSON) | Ethernet/WiFi |
| PC ↔ RK3506 (TCP) | TCP (JSON) | Ethernet/WiFi |
| RK3506 ↔ STM32F407 | 自定义二进制帧 | UART (TTL) |
| STM32F407 ↔ OLED | SSD1306 命令协议 | I²C |

### 5.2 WebSocket JSON 指令格式
**下行 (PC → RK3506 → STM32)**:
```json
{"cmd": "led",         "data": {"state": 1}}
{"cmd": "mode",        "data": {"mode": 3}}
{"cmd": "text",        "data": {"content": "你好世界"}}
{"cmd": "weather",     "data": {"type": 0, "temp": 26, "humidity": 65, "wind": 0}}
{"cmd": "time_sync",   "data": {"ts": 1753776000}}
{"cmd": "boot_text",   "data": {"content": "欢迎进入系统"}}
{"cmd": "serial_test", "data": {"content": "测试消息"}}
```

**上行 (STM32 → RK3506 → PC)**:
```json
{"evt": "led_status",      "data": {"state": 1}}
{"evt": "mode_status",     "data": {"mode": 3}}
{"evt": "key_event",       "data": {"key": 1, "action": "press"}}
{"evt": "frame_sync",      "data": "<Base64 编码的 1024 字节显存>"}
{"evt": "ack",             "data": {"code": 0}}
{"evt": "error",           "data": {"code": 3}}
```

**下行 ACK 响应格式** (RK3506 → PC, JSON 命令的立即响应):
```json
{"cmd": "ack", "data": {"ref": "led", "code": 0}}
```
- `ref`: 原始命令名
- `code`: 0=成功, -1=参数错误, -2=未知命令, 正数=串口发送 seq 号

---

## 六、开发阶段规划建议

| 阶段 | 内容 | 关键产出 |
|------|------|----------|
| **阶段1** | STM32F407 OLED 驱动 | SSD1306 驱动 + 英文/中文显示 + 全部特效 |
| **阶段2** | STM32F407 串口帧协议 | UART 中断收发 + 帧解析 + 命令执行 + LED/按键 |
| **阶段3** | RK3506 串口通信 | Linux 串口编程 + 帧收发 + 协议转换 |
| **阶段4** | RK3506 Web/TCP 服务器 | HTTP/WebSocket 服务 + 前端页面 + JSON 协议 |
| **阶段5** | 全链路联调 | 端到端测试 + OLED 显存同步 + 预览验证 |
| **阶段6** | 调试能力建设 | STM32 调试串口 + 字模生成工具 + 串口测试命令 |

### 6.1 开发工具链
| 端 | 编译器 | 工具 |
|----|--------|------|
| RK3506 | `arm-linux-gcc` (Buildroot 交叉工具链) | make + SCP 部署 |
| STM32F407 | `arm-none-eabi-gcc` (GNU Arm Embedded) | CubeMX + make + st-flash |
| 字模生成 | Python 3 + Pillow | `tools/gen_font.py` → 覆盖 `src/font.c` |

---

## 七、硬件 BOM

| 物料 | 数量 | 说明 |
|------|------|------|
| RK3506 开发板 | 1 | 运行 Linux |
| STM32F407 开发板 | 1 | 如 STM32F407G-DISC1 |
| 0.96" OLED 模块 | 1 | SSD1306 驱动, 128×64, I²C 接口 |
| 杜邦线 | 若干 | UART 连接 (TX↔RX, RX↔TX, GND) + I²C 连接 (SCL, SDA, VCC, GND) |
| 网线 / WiFi 模块 | 1 | RK3506 联网 |

---

## 八、关键注意事项

1. **UART 电平匹配**: RK3506 与 STM32F407 均为 TTL 电平 (3.3V)，可直接连接，无需电平转换
2. **I²C 上拉电阻**: OLED 模块通常已集成上拉电阻，但需确认；如无则外接 4.7kΩ
3. **OLED 中文取模**: 使用 PC 端字模工具（如 PCtoLCD2002），生成 16×16 点阵，按 GB2312 区位码排列
4. **帧协议 CRC**: 使用 CRC-8-ATM，多项式 `x^8 + x^2 + x + 1` (0x07)
5. **超时重传**: 所有上行下行指令均需 ACK，500ms 超时重传，最多 3 次，3 次失败则上报通信异常
6. **OLED 实时预览**: Canvas 绘制时需要模拟 SSD1306 的页寻址模式和颜色反转特性，确保预览画面与实物一致

---

## 九、软件架构设计

### 9.1 设计原则
- **分层解耦**: 每层仅依赖下层抽象接口，不依赖具体实现
- **面向接口编程**: 外设驱动、通信协议、显示模块均通过接口层隔离
- **便于扩展**: 新增显示特效或通信指令时，只需扩展对应模块，不影响其他模块
- **最小耦合**: 模块间通过事件/回调通信，避免直接调用

### 9.2 STM32F407 软件架构

```
┌─────────────────────────────────────────────────────────────┐
│                     main.c (主循环 + 初始化)                   │
├─────────────────────────────────────────────────────────────┤
│  应用层 (Application Layer)                                  │
│  ┌───────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ led_mgr   │  │ display_mgr  │  │ system_config        │  │
│  │ LED管理器  │  │ 显示管理器    │  │ 系统配置(Flash存取)   │  │
│  └─────┬─────┘  └──────┬───────┘  └──────────┬───────────┘  │
├────────┼───────────────┼──────────────────────┼──────────────┤
│  协议层 (Protocol Layer)                                     │
│  ┌─────┴───────────────┴──────────────────────┴───────────┐  │
│  │                  protocol_engine                        │  │
│  │  协议引擎: 命令解析/分发/ACK/NAK/重传/帧编解码           │  │
│  └──────────┬──────────────────────────────┬──────────────┘  │
├─────────────┼──────────────────────────────┼─────────────────┤
│  驱动抽象层 (Hardware Abstraction Layer)                    │
│  ┌──────────┴──────┐  ┌─────────┐  ┌──────┴──────────────┐  │
│  │ uart_drv        │  │ i2c_drv │  │ flash_drv           │  │
│  │ 串口驱动(中断+DMA)│  │ I²C驱动 │  │ Flash读写驱动        │  │
│  └────────┬────────┘  └────┬────┘  └─────────┬───────────┘  │
├───────────┼────────────────┼─────────────────┼──────────────┤
│  BSP 层 (Board Support Package)                             │
│  ┌────────┴───────┐  ┌─────┴──────┐  ┌──────┴────────────┐  │
│  │ gpio_drv       │  │ key_drv    │  │ iwdg_drv          │  │
│  │ GPIO驱动       │  │ 按键驱动    │  │ 看门狗驱动(条件编译)│  │
│  └────────────────┘  └────────────┘  └───────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  STM32 HAL 库                                               │
└─────────────────────────────────────────────────────────────┘
```

**各模块职责**:
| 模块 | 文件 | 职责 |
|------|------|------|
| `display_mgr` | `display_mgr.c/h` | 显示模式管理、区域划分、特效调度，依赖 `ssd1306` |
| `ssd1306` | `ssd1306.c/h` | OLED 底层驱动（I²C 命令/数据发送、页/列寻址） |
| `font` | `font.c/h` | 字库管理（ASCII 5×7/8×16、中文 16×16 取模） |
| `protocol_engine` | `protocol.c/h` | 二进制帧封装/解析、CRC 校验、ACK 超时重传 |
| `uart_drv` | `uart_drv.c/h` | UART 环形缓冲区（中断接收 + DMA 发送） |
| `flash_drv` | `flash_drv.c/h` | STM32 内部 Flash 扇区擦除/写入/读取底层操作 |
| `led_mgr` | `led_mgr.c/h` | LED 状态机（关/开/闪烁, 闪烁默认 500ms 周期） |
| `key_drv` | `key_drv.c/h` | 按键扫描 + 消抖 + 长按检测 |
| `system_config` | `sys_config.c/h` | Flash 参数存储区读写（默认文字等） |
| `iwdg_drv` | `iwdg_drv.c/h` | IWDG 初始化与喂狗（受 `IWDG_ENABLE` 宏控制） |
| `debug_console` | `debug_console.c/h` | 独立调试串口输出（受 `DEBUG_UART_ENABLE` 宏控制） |

**扩展点设计**:
- **新增显示特效**: 在 `display_mgr` 中注册新特效函数指针，`effects[]` 数组追加一项即可
- **新增通信指令**: 在 `protocol_engine` 的命令分发表 `cmd_table[]` 中添加一项 + 实现处理函数
- **新增传感器/外设**: 建立独立驱动模块，通过 `protocol_engine` 挂载事件上报

### 9.3 RK3506 软件架构

```
┌─────────────────────────────────────────────────────────────┐
│                     main.c (事件循环)                         │
├─────────────────────────────────────────────────────────────┤
│  业务逻辑层 (Business Layer)                                  │
│  ┌──────────────────┐  ┌────────────────┐  ┌──────────────┐ │
│  │ cmd_dispatcher   │  │ (天气内置)     │  │ (时间内置)   │ │
│  │ 命令转发/转换     │  │ cmd_dispatcher │  │cmd_dispatcher│ │
│  └────────┬─────────┘  └───────┬────────┘  └──────┬───────┘ │
├───────────┼────────────────────┼──────────────────┼─────────┤
│  通信层 (Communication Layer)                                 │
│  ┌────────┴──────────┐  ┌──────┴────────┐  ┌──────┴───────┐ │
│  │ web_server        │  │ tcp_server    │  │ uart_adapter │ │
│  │ HTTP+WebSocket    │  │ TCP Socket    │  │ 串口适配器    │ │
│  └───────────────────┘  └───────────────┘  └──────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  OS 抽象层 → Linux (socket, termios, epoll/pthread)           │
└─────────────────────────────────────────────────────────────┘
```

**各模块职责**:
| 模块 | 文件 | 语言 | 职责 |
|------|------|------|------|
| `web_server` | `web_server.c/h` | C | HTTP 静态资源服务 + WebSocket 管理（基于 mongoose） |
| `tcp_server` | `tcp_server.c/h` | C | TCP 客户端连接管理 + JSON 收发（基于 POSIX socket） |
| `uart_adapter` | `uart_adapter.c/h` | C | 串口二进制帧收发 + ACK 超时管理（基于 termios） |
| `cmd_dispatcher` | `cmd_dispatcher.c/h` | C | WebSocket/TCP → 串口帧转换，STM→Web 事件推送，内置天气模拟、时间同步、OLED 显存重组 |
| `protocol` | `protocol.c/h` | C | 二进制帧协议编解码 + CRC8 校验状态机 |

**语言说明**: RK3506 全部模块使用纯 C (C11 标准) 开发，仅依赖 POSIX C 标准库 (libc)、pthread 和 mongoose 单头文件库，无 C++ 运行时依赖。

**扩展点设计**:
- **新增 WebSocket 事件**: 在 `cmd_dispatcher` 中追加事件类型即可
- **替换底层 HTTP 库**: 仅修改 `web_server` 模块内部实现，接口不变
- **后续 OTA 升级**: 新增 `ota_handler` 模块，挂载到 `cmd_dispatcher` + `uart_adapter` 二进制传输通道
- **所有 RK3506 模块均为纯 C (C11) 实现**，无 C++ 运行时依赖，适合 Buildroot 等轻量级 Linux 环境

### 9.4 OTA 固件升级预留
- **协议层预留**: 二进制帧协议已预留 `0x07` 命令码用于 OTA
- **STM32 端预留**: Flash 分区规划中预留 Bootloader 区域（16KB）和 App 区域，当前 App 使用剩余 Flash
- **RK3506 端预留**: 架构上预留 `ota_handler` 模块接口
- **本次不做实现**，后续需要时只需补充：
  - STM32: Bootloader 代码（Flash 擦写 + 跳转）
  - RK3506: 固件文件读取 + 分片传输 + 完整性校验
  - 协议: `0x07` 命令完整定义（分片序号、MD5 校验等）

---

## 十、确认与决策记录

| 编号 | 决策项 | 结论 |
|------|--------|------|
| D1 | RK3506 OS | Linux (Buildroot) |
| D2 | 服务器类型 | Web 为主 (HTTP+WebSocket), TCP 为备选 (端口 9527) |
| D3 | 天气数据来源 | RK3506 本地模拟，不联网 |
| D4 | LED 控制 | 板载 LED GPIO 开关/闪烁 |
| D5 | OLED 显示模式 | 全部 7 种特效 (静态+4向滚动+翻页+淡入淡出) |
| D6 | 通讯参数 | UART 115200/8N1, TCP 由系统推荐 |
| D7 | STM32 固件 | 裸机 HAL 库 |
| D8 | 串口协议 | 自定义二进制帧 (SOF+LEN+CMD+SEQ+DATA+CRC8+EOF) |
| D9 | 中文显示 | 需要, GB2312 16×16 点阵取模存 Flash |
| D10 | 互联网 | 不需要 |
| D11 | 上位机方式 | Web 为主, TCP 为备选调试 |
| D12 | OLED 布局 | 多区域组合 (顶部状态栏 + 主内容区) |
| D13 | Web 功能 | 完整仪表盘 (连接/LED/模式/文字/天气/按键/预览) |
| D14 | 显示切换 | Web 指令 + STM32 板载按键 |
| D15 | 上电默认显示 | "欢迎进入系统", Flash 存储, 可通过指令修改 |
| D16 | 看门狗 | 使能 IWDG, 通过 `IWDG_ENABLE` 编译宏控制开关 |
| D17 | OTA 升级 | 预留但不实现, 协议 + 代码架构已留扩展点 |
| D18 | 代码架构 | 分层解耦 + 面向接口编程, 支持扩展 |
| D19 | OLED 实时预览 | 通过 `CMD_FRAME_SYNC` 分片上传显存，Base64 编码推送 Web Canvas |
| D20 | STM32 调试串口 | 独立 USART2 仅 TX，`DEBUG_UART_ENABLE` 宏控制编译开关 |
| D21 | 字模生成 | Python 工具 `gen_font.py` 自动化生成，覆盖 `font.c` |
| D22 | 串口测试 | Web 端 `serial_test` 命令，RK3506 控制台输出 + 转发至 STM32 |
| D23 | 时间编码方式 | 二进制原始值编码（年=year-2000），非 BCD 码 |



---

**文档版本**: v1.3  
**创建日期**: 2026-07-29  
**最后更新**: 2026-07-30  
**状态**: 已评审 — 最终版（已根据代码实现同步更新）

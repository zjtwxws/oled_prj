## OLED 串口直连控制项目需求规格 (v3.1)

> v3.0 → v3.1 核心变更：新增**本地/远程双模式**。本地模式 STM32 可脱离 PC 独立运行（文字+7种特效）；远程模式 PC 全控（渲染帧缓冲下发，支持时间/天气/日期等）。两者可通过上位机命令或板载按键切换。

---

## 一、项目概述

### 1.1 项目目标
搭建一个 Windows 串口上位机直连 STM32F407 控制 OLED 显示的桌面系统。

### 1.2 运行模式

系统支持两种运行模式，通过上位机命令或板载按键切换：

#### 本地模式 (Local Mode)
```
STM32F407 ──I²C── OLED 0.96" 128×64
    ↑ SSD1306
    本地渲染文字+7种特效
    无状态栏, 无LED显示
    PC断开时独立工作
```

- STM32 自行渲染文字内容及 7 种显示特效（静态/左滚/右滚/上滚/下滚/翻页/淡入淡出）
- 全屏 128×64 渲染，无状态栏
- OLED 不显示 LED 信息
- 上电默认文字从 Flash 读取显示
- 板载按键可切换特效模式、切换至远程模式

#### 远程模式 (Remote Mode)
```
Windows PC (Win32 对话框) ──USB-TTL── STM32F407 ──I²C── OLED 0.96" 128×64
     ↑ 二进制帧协议               ↑ USART1             ↑ SSD1306
     PC渲染帧缓冲→分段下发          PA9/PA10              STM32仅刷屏
     115200/8N1
```

- PC 端全屏渲染 128×64 帧缓冲（1024 字节）
- 帧缓冲通过 `CMD_FRAME_SYNC`(0x20) 分段发送（每段 ≤200 字节）
- STM32 接收帧缓冲分段，拼装后直接写入 SSD1306，**不自行渲染任何内容**
- 支持文字/时间时钟/天气/日期等多种显示模式
- 所有特效动画在 PC 端 GDI 预览中完成

### 1.3 与 v3.0 版本差异

| 项目 | v3.0 | v3.1 |
|------|------|------|
| 运行模式 | 仅远程模式 | **本地/远程双模式** |
| 本地能力 | STM32 无自主渲染 | STM32 可独立运行文字+7种特效 |
| 模式切换 | — | 上位机命令 + 板载按键 |
| display_mgr | 纯帧缓冲接收+刷屏 | **双模式**：本地自行渲染 / 远程帧缓冲刷屏 |
| 特效位置 | PC 端（远程模式） | 远程→PC, 本地→STM32 |
| 帧缓冲渲染 | 始终启用 | 仅远程模式启用 |
| 离线运行 | 不支持 | 本地模式支持拔掉 USB-TTL 独立工作 |

---

## 二、Windows 上位机详细规格

### 2.1 技术选型

| 项目 | 选择 | 理由 |
|------|------|------|
| IDE | VS2019 (v142) | 用户指定 |
| 语言标准 | C++17 | 现代特性+稳定 |
| 界面框架 | Win32 对话框 (`DialogBox`) | 零额外依赖，与用户现有 MAX7219 项目一致 |
| 串口库 | Win32 API (`CreateFile`/`ReadFile`/`WriteFile`) | 零依赖 |
| 线程模型 | 主线程(UI) + 串口 Overlapped I/O | `FILE_FLAG_OVERLAPPED` 异步读 |
| OLED 预览 | GDI (`CreateCompatibleDC`/`StretchBlt`/`SetPixel`) | 本地软件模拟 |
| 链接库 | `comctl32.lib` `setupapi.lib` | Common Controls + 串口枚举 |
| 中文字符 | `L"..."` 宽字面量 + UTF-8 BOM 源文件 | MSVC 正确识别 |

### 2.2 项目文件结构

```
pc_host/
├── oled_control.sln
└── oled_control/
    ├── oled_control.vcxproj          # Win32Proj, v142, C++17, Unicode
    ├── oled_control.vcxproj.filters
    ├── oled_control.rc               # 对话框资源 (UTF-8 BOM)
    ├── resource.h                     # 控件 ID 定义
    ├── res/oled_control.ico
    ├── protocol.h / .cpp             # 二进制帧协议 (CRC-8-ATM, 状态机)
    ├── serial_port.h / .cpp          # Win32 串口驱动
    ├── frame_queue.h                  # 线程安全环形队列 + TxTask
    ├── oled_preview.h / .cpp         # GDI OLED 预览控件 (WndProc)
    ├── oled_preview_gen.h            # 字模数据 (316 汉字 + ASCII + 天气图标)
    └── main.cpp                       # WinMain + DialogProc
```

### 2.3 主界面布局

```
┌─────────────────────────────────────────────────────────┐
│  OLED 控制中心                              COM3 ● 已连接 │
├────────────────────┬────────────────────────────────────┤
│  串口设置            │                                    │
│  端口: [COM3 ▼]      │    ┌────────────────────────────┐  │
│  [打开] [关闭]       │    │    OLED 模拟预览             │  │
│                      │    │    128×64 GDI 4x 放大        │  │
│  运行模式             │    │    深灰底+亮黄绿像素          │  │
│  ● 远程 ○ 本地        │    │    远程:帧缓冲渲染下发       │  │
│                      │    └────────────────────────────┘  │
│  LED 控制            │                                    │
│  ● 关 ○ 开 ○ 闪烁    │    ┌────────────────────────────┐  │
│                      │    │  设备状态                    │  │
│  显示模式 (远程时)    │    │  模式: 本地/远程             │  │
│  ● 文字 ○ 时间        │    │  子模式: 文字               │  │
│  ○ 天气 ○ 日期        │    │  LED: 关                   │  │
│  文字内容              │    │  延迟: -- ms                │  │
│  ┌────────────────┐ │    └────────────────────────────┘  │
│  │ (多行输入)      │ │                                    │
│  └────────────────┘ │    ┌────────────────────────────┐  │
│  [发送文字]          │    │  按键日志                    │  │
│                      │    │  [HH:MM:SS] KEYx 短按       │  │
│  上电默认文字         │    │  ...(最多100条, 自动滚动)   │  │
│  [欢迎进入系统] [保存] │    └────────────────────────────┘  │
│                      │                                    │
│  天气设置 (天气模式用) │                                    │
│  类型:[晴▼] 温度:[25]℃│                                   │
│  湿度:[65]% 风向:[北▼] │                                    │
│  [发送天气]           │                                    │
│                      │                                    │
│  [同步系统时间]        │                                    │
└──────────────────────┴────────────────────────────────────┘
```

### 2.4 显示模式规格

#### 2.4.1 本地模式 (Local Mode)
本地模式由 STM32 自行渲染，不需要 PC 参与：
- 全屏 128×64 渲染用户文字内容（从 Flash 读取上电默认文字）
- 支持 7 种特效：静态/左滚/右滚/上滚/下滚/翻页/淡入淡出
- 无状态栏，OLED 不显示 LED 信息，不显示时间/天气
- 板载按键可切换特效和远程模式
- 拔掉 USB-TTL 后独立工作

#### 2.4.2 远程模式 — 文字 (TEXT)
- PC 端全屏渲染文字→帧缓冲分段下发→STM32 刷屏
- 支持 7 种特效（PC 端 GDI 渲染）
- 特效刷新率：滚动 25fps, 翻页 3s, 淡入淡出 32 级 @ 30ms

#### 2.4.3 远程模式 — 时间 (CLOCK)
- PC 端渲染大字时钟（如 `12:30` 居中全屏）→帧缓冲下发
- 每秒刷新，支持秒显示 `12:30:45`
- 支持特效切换

#### 2.4.4 远程模式 — 天气 (WEATHER)
- PC 端渲染全屏天气信息→帧缓冲下发
- 数据来源：用户在 PC 端设置

#### 2.4.5 远程模式 — 日期 (DATE)
- PC 端渲染年/月/日/星期→帧缓冲下发
- 数据来源：PC 系统时间

### 2.5 各模块功能规格

#### 2.5.1 串口管理
- `SerialPort::Enumerate()` 枚举 COM1~COM16，`SetupDiGetClassDevs` 获取友好名称
- 参数：115200/8N1/无流控/DTR+RTS 使能
- 读超时：`ReadIntervalTimeout=50ms`, `ReadTotalTimeoutConstant=100ms`
- 发送：同步 `WriteFile`（数据极小，不阻塞 UI）
- 接收：`FILE_FLAG_OVERLAPPED` + `CreateThread`，通过 `SerialPort::onDataReceived` 回调

#### 2.5.2 二进制帧协议
`ProtocolParser` 类与 STM32 端 `proto_feed_byte()` 完全一致：
- 7 状态状态机：`WAIT_SOF → LEN → CMD → SEQ → DATA → CRC → EOF`
- `FeedByte(uint8_t)` 逐字节喂入，返回到 true 表示帧完成
- `BuildFrame()` 构建发送帧（SOF+LEN+CMD+SEQ+DATA+CRC8+EOF）
- `CheckTimeout()` 500ms 超时自动复位状态机
- CRC8 查表与 STM32 端 `crc8_table[256]` 完全一致（多项式 0x07）

命令码（v3.1 更新）：
| 命令码 | 名称 | 方向 | 说明 |
|--------|------|------|------|
| 0x01 | LED_CTRL | PC→STM | LED 控制（关/开/闪烁），仅物理 LED，OLED 不显示 |
| 0x02 | DISPLAY_MODE | PC→STM | 切换显示模式（本地:仅TEXT, 远程:0=文字,1=时间,2=天气,3=日期） |
| 0x03 | TEXT_CONTENT | PC→STM | 文字内容（UTF-8），本地/远程文字模式均可用 |
| 0x04 | TIME_SYNC | PC→STM | 时间同步（7 字节），远程时间/日期模式使用 |
| 0x05 | WEATHER_DATA | PC→STM | 天气数据（4 字节），远程天气模式使用 |
| 0x06 | BOOT_TEXT | PC→STM | 上电默认文字，写入 Flash（本地模式启动时读取） |
| 0x07 | OTA_RESERVED | — | 预留 |
| 0x10 | LED_STATUS | STM→PC | LED 状态上报 |
| 0x11 | MODE_STATUS | STM→PC | 模式上报（本地/远程 + 子模式） |
| 0x12 | KEY_EVENT | STM→PC | 按键事件（含切换本地/远程模式按键） |
| 0x20 | FRAME_SYNC | PC→STM | **远程模式帧缓冲分段下发**：`[seg][total][payload≤200B]` |
| 0xF0 | ACK | STM→PC | 应答确认 |
| 0xFF | NAK | STM→PC | 应答否定 |

#### 2.5.3 本地/远程模式切换
- **上位机切换**：`CMD_DISPLAY_MODE`(0x02) 携带模式字，bit7=0 本地 / bit7=1 远程，低 7 位为子模式
- **按键切换**：板载 KEY1~4 中指定按键组合（如 KEY4 长按），切换本地/远程模式
- **自动检测**：STM32 检测串口连接状态——PC 断开超时自动切换至本地模式
- 模式切换时 STM32 通过 `CMD_MODE_STATUS`(0x11) 上报当前状态

#### 2.5.4 帧缓冲渲染引擎（远程模式）
`FrameRenderer` 类职责：
- 维护 128×64 帧缓冲（1024 字节，按 SSD1306 page 格式排列）
- 根据远程模式下选中的子模式渲染全屏→帧缓冲分段下发
- 每种模式下均支持 7 种特效动画
- 分段策略：每段 payload ≤200 字节，1024 字节 ≈ 6 段
- 分段格式：`[seg编号(1B)][总段数(1B)][帧缓冲数据(≤200B)]`
- 仅远程模式启用帧缓冲传输，本地模式不使用

#### 2.5.5 OLED 模拟预览
`OledPreview` 类变更：
- **本地模式预览**：仅显示文字内容+特效（模拟 STM32 本地渲染效果），无状态栏
- **远程模式预览**：全屏渲染（TEXT/CLOCK/WEATHER/DATE），与帧缓冲引擎共享渲染管线
- `SetMode(isRemote, subMode)` 新增：切换本地/远程及子模式
- 每种模式均支持 7 种特效（在预览中直接看到动画效果）
- `Tick()` 方法由主窗口 `WM_TIMER(50ms)` 驱动
- 其他不变：128×64 内存位图 → `StretchBlt` 4x 放大，深灰底+亮黄绿像素

## 三、STM32F407 端需求

### 3.1 角色变更 (v3.0)
STM32 由"渲染+刷屏"退化为**纯帧缓冲接收+刷屏**角色：
- **本地模式**：自行渲染文字内容+7种特效（全屏 128×64，无状态栏，无 LED 显示）
- **远程模式**：接收 PC 下发的帧缓冲分段 → 拼装 → `ssd1306_update_screen()`（不自行渲染）
- 保留：协议解析、LED 控制、按键检测、Flash 配置管理、看门狗

### 3.2 display_mgr 重构 (v3.0→v3.1)
| 变更项 | v3.0 (仅远程) | v3.1 (本地+远程) |
|--------|------|------|
| 运行模式 | 仅远程帧缓冲刷屏 | **双模式**：本地自行渲染 / 远程帧缓冲刷屏 |
| 状态栏 | 删除 | **删除**（两端均不显示状态栏） |
| 文字绘制 | 删除 | **保留**（本地模式需要 `draw_text_content()`） |
| 7 种特效 | 删除 | **保留**（本地模式需要 `effect_*()`） |
| 帧缓冲接收 | `display_mgr_rx_frame_seg()` | **保留**（远程模式使用） |
| 模式管理 | 仅记录模式号 | 记录本地/远程 + 子模式 + 模式切换逻辑 |
| 新增接口 | — | `display_mgr_set_remote(bool)` 切换本地/远程 |

### 3.3 固件变更清单 (v3.0 → v3.1)

| 文件 | 改动 |
|------|------|
| `display_mgr.h` | 新增 `display_mgr_set_remote(bool)` / `display_mgr_is_remote()`；保留本地渲染接口 |
| `display_mgr.c` | 双模式调度：`tick()` 中根据 `is_remote` 走不同分支；本地模式走特效 tick，远程模式仅等待帧缓冲 |
| `user_app.c` | 新增模式切换处理；`CMD_FRAME_SYNC` 仅在远程模式生效；按键事件中增加模式切换键 |
| `menu_mgr.h` / `menu_mgr.c` | **新增**: 多级菜单导航状态机+OLED 渲染, 支持 SUBMENU/TOGGLE/VALUE/ACTION/INFO 5种菜单项 |
| `menu_items.c` | **新增**: 菜单树静态数据定义 (8项主菜单, 全部 const 存于 Flash) |
| `key_drv.h` / `key_drv.c` | **新增** `KEY_EVENT_LONG_PRESS_REPEAT` 事件; 长按检测改用 HAL_GetTick 真实时间 |
| `display_mgr.h` / `display_mgr.c` | 新增 `display_mgr_redraw()` / `display_mgr_set_menu_suppress()`; 菜单激活期间抑制远程帧刷屏 |
| `protocol.h` | `CMD_MODE_STATUS`(0x11) 数据格式更新：`[is_remote(1B)][sub_mode(1B)]` |

### 3.4 外设配置（不变）
| 外设 | 引脚 | 参数 |
|------|------|------|
| USART1 (PC通信) | PA9(TX), PA10(RX) | 115200/8N1 |
| USART2 (调试) | PA2(TX), PA3(RX) | 115200/8N1, `DEBUG_UART_ENABLE` 控制 |
| I²C2 (OLED) | PB10(SCL), PB11(SDA) | Fast Mode 400kHz |
| LED | PF9 | 推挽输出 |
| KEY1~4 | PE1~PE4 | 上拉输入 |
| IWDG | — | `IWDG_ENABLE` 控制 |

---

## 四、通信协议

```
┌──────┬──────┬──────┬──────┬────────────┬──────┬──────┐
│ SOF  │ LEN  │ CMD  │ SEQ  │   DATA     │ CRC8 │ EOF  │
│ 0xA5 │ 1B   │ 1B   │ 1B   │  0~251B    │ 1B   │ 0x5A │
└──────┴──────┴──────┴──────┴────────────┴──────┴──────┘
```

- CRC-8-ATM: 多项式 0x07，初始值 0x00，覆盖 SOF~DATA
- PC 为主设备，STM32 收到指令回复 ACK/NAK
- STM32 主动上报按键事件 (0x12)，PC 回复 ACK
- 500ms 超时重传，最多 3 次

### 4.1 帧缓冲分段协议 (CMD_FRAME_SYNC) — 远程模式专用

```
PC → STM32:  [SOF][LEN][0x20][SEQ][seg][total][payload(N≤200)][CRC8][EOF]
                                                 └── 帧缓冲片段 ──┘
```

- **仅在远程模式下使用**，本地模式不发送 `CMD_FRAME_SYNC`
- `seg`：当前段编号（从 0 开始）
- `total`：总段数
- `payload`：帧缓冲数据片段，每段 ≤200 字节
- 总共 1024 字节（128×64÷8），约 6 段
- STM32 收到 `seg=0` 时清空帧缓冲，收到最后一帧时拼装完成自动刷屏
- 本地模式下 STM32 收到 `CMD_FRAME_SYNC` 应忽略（或回复 NAK）

---

## 五、编译与运行

### 5.1 Windows 上位机
```
1. VS2019 打开 pc_host/oled_control.sln
2. 选择 Debug/Release + Win32/x64
3. 生成 → 产物 pc_host/oled_control/Debug/oled_control.exe

命令行编译：
MSBuild pc_host/oled_control.sln /t:Build /p:Configuration=Debug /p:Platform=Win32
```

### 5.2 STM32F407
```bash
cd stm32f407
make
make flash
```

### 5.3 设备连接
```
USB-TTL TX → PA10 (USART1 RX)
USB-TTL RX → PA9  (USART1 TX)
USB-TTL GND → GND

OLED SCL → PB10 | SDA → PB11 | VCC → 3.3V | GND → GND
```

---

## 六、关键注意事项

1. 所有源文件必须为 UTF-8 BOM 编码（MSVC CP936 环境下的中文兼容要求）
2. `#define _WIN32_WINNT 0x0601` 必须出现在 `<windows.h>` 之前（启用 `GetTickCount64`）
3. `windowsx.h` 提供 `ComboBox_AddString`、`Button_GetCheck` 等控件宏
4. **v3.1 双模式**：本地模式 STM32 自行渲染文字+特效，远程模式 PC 渲染帧缓冲下发
5. `CMD_FRAME_SYNC`(0x20) 仅远程模式启用，本地模式不使用
6. OLED 预览与帧缓冲引擎使用同一渲染管线，确保所见即所得
7. 帧缓冲分段每段 payload ≤200 字节（含 seg+total 头共 ≤202 字节），保证每帧在 115200 波特率下约 17ms 发送一帧
8. RK3506 目录保留为预留
9. 串口枚举依赖 `setupapi.lib`
10. 本地模式下 STM32 完全独立，拔掉 USB-TTL 后仍正常运行

---


---

## 七、多级菜单系统 (v3.1 新增)

### 7.1 概述

STM32 端集成轻量级多级菜单系统 (`menu_mgr`)，通过 4 个板载按键导航，在 128×64 OLED 上实现完整的设备配置界面。

### 7.2 菜单结构

`
主菜单 (8项, 上电自动进入)
├── 1.工作模式 → 本地/远程 (TOGGLE 互斥)
├── 2.显示内容 → 时间/天气/日期/自定义文字 (ACTION)
├── 3.显示特效 → 静态/左滚/右滚/上滚/下滚/翻页/淡入淡出 (ACTION)
├── 4.设置 → 对比度设置 → 对比度 (VALUE 0~255, step=5)
├── 5.LED控制 → 关闭/常亮/闪烁 (ACTION)
├── 6.上电文字 (INFO, 预留)
├── 7.系统信息 → 固件版本/运行时间 (INFO)
└── 8.预留 → 三级示例 → 选项A/B/C (ACTION, 仅DEBUG_PRINTF)
`

### 7.3 按键映射

| 按键 | 短按 | 长按 |
|------|------|------|
| KEY1 (PE1) | **向下** — 光标下移 / VALUE编辑时值增加 | 快速连续下翻 (每 150ms 重复) |
| KEY2 (PE2) | **向上** — 光标上移 / VALUE编辑时值减少 | 快速连续上翻 (每 150ms 重复) |
| KEY3 (PE3) | **确认** — 进入子菜单/执行动作/TOGGLE切换 | — |
| KEY4 (PE4) | **返回** — 返回上级/根菜单退出 | 返回根菜单/根菜单退出；正常显示中长按激活菜单 |

### 7.4 新增文件

| 文件 | 职责 |
|------|------|
| `inc/menu_mgr.h` | 菜单项数据结构 + 公开 API |
| `src/menu_mgr.c` | 导航状态机 + OLED 反白渲染 + VALUE 编辑 |
| `src/menu_items.c` | 菜单树静态定义 + 回调函数 (桥接到 display_mgr/led_mgr/ssd1306) |

### 7.5 关键设计

- 菜单数据全部 `const` 存于 Flash (.rodata), RAM 仅 ~60 字节
- 选中行反白渲染: 先填充白色背景再擦除文字
- 进入子菜单时压栈保存父级光标位置, 返回时恢复
- 菜单激活期间 `display_mgr` 抑制远程帧刷屏, 退出后 redraw 恢复
- VALUE 编辑时 KEY4 恢复原始值, KEY3 确认
- INFO 模式下任意键退出

详细设计参见 `docs/menu-design.md`。

---

## 八、确认与决策记录


| 编号 | 决策项 | 结论 | 版本 |
|------|--------|------|------|
| D1 | 上位机框架 | VS2019 + Win32 对话框（非 MFC） | v2.0 |
| D2 | OLED 预览 | 本地 GDI 软件模拟 | v2.0 |
| D3 | RK3506 | 目录保留为预留 | v2.0 |
| D4 | 通信协议 | 沿用 v1.4 二进制帧协议 | v2.0 |
| D5 | C++ 标准 | C++17 | v2.0 |
| D6 | 编码方案 | UTF-8 BOM | v2.0 |
| D7 | 串口库 | Win32 API（零依赖） | v2.0 |
| D8 | 双模式架构 | 本地模式(STM32自渲染)+远程模式(PC渲染帧缓冲) | v3.1 |
| D9 | OLED 状态栏 | 本地/远程均取消常驻状态栏 | v3.1 |
| D10 | 远程显示模式 | 文字/时间/天气/日期（PC 渲染帧缓冲） | v3.0 |
| D11 | 本地模式能力 | 仅文字内容+7种特效，无时间/天气/日期 | v3.1 |
| D12 | LED 显示 | OLED 不显示 LED，仅物理 LED 灯 | v3.0 |
| D13 | CMD_FRAME_SYNC | 仅远程模式启用：PC→STM32 帧缓冲分段下发 | v3.1 |
| D14 | 模式切换方式 | 上位机命令 + 板载按键 + 串口断开自动切换 | v3.1 |
| D15 | 特效位置 | 远程→PC端, 本地→STM32端 | v3.1 |
| D16 | 多级菜单 | 自制轻量 menu_mgr (非 LVGL), Flash ~3KB, RAM ~60B | v3.1 |
| D17 | 按键方向 | KEY1=下, KEY2=上 (以代码实现为准) | v3.1 |
| D18 | 菜单激活方式 | KEY4 长按激活菜单; 原 KEY4 长按切换模式功能移至菜单内操作 | v3.1 |

---

**文档版本**: v3.1
**创建日期**: 2026-07-29
**最后更新**: 2026-08-05
**状态**: 核心功能已实现
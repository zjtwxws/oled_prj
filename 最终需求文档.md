## OLED 串口直连控制项目需求规格 (v2.0)

> v1.4 → v2.0 核心变更：去掉 RK3506 网关层，Windows PC 通过 USB-TTL 串口直连 STM32F407。上位机从 MFC 改为纯 Win32 API 对话框应用。

---

## 一、项目概述

### 1.1 项目目标
搭建一个 Windows 串口上位机直连 STM32F407 控制 OLED 显示的桌面系统。

### 1.2 系统拓扑

```
Windows PC (Win32 对话框) ──USB-TTL── STM32F407 ──I²C── OLED 0.96" 128×64
     ↑ 二进制帧协议               ↑ USART1             ↑ SSD1306
     SOF+LEN+CMD+SEQ+DATA+CRC8+EOF  PA9/PA10              PB10/PB11
     115200/8N1
```

### 1.3 与 v1.4 版本差异

| 项目 | v1.4 | v2.0 |
|------|------|------|
| 架构 | PC→RK3506→STM32 | PC→STM32 |
| 连接方式 | 浏览器 HTTP/WebSocket | USB-TTL 串口 |
| 上位机技术 | HTML5/CSS/JS | VS2019 + Win32 API + C++17 |
| 界面框架 | — | 纯 Win32 对话框（无 MFC） |
| 通信协议 | JSON + 二进制帧 | 仅二进制帧 |
| OLED 预览 | Web Canvas (读取显存) | GDI 本地软件模拟 |
| 工程类型 | — | `<Keyword>Win32Proj</Keyword>` |
| 编码 | — | UTF-8 BOM（MSVC CP936 兼容） |

---

## 二、Windows 上位机详细规格

### 2.1 技术选型

| 项目 | 选择 | 理由 |
|------|------|------|
| IDE | VS2019 (v142) | 用户指定 |
| 语言标准 | C++17 | 现代特性+稳定 |
| 界面框架 | Win32 对话框 (`DialogBox`) | 零额外依赖，与用户现有 MAX7219 项目一致 |
| 串口库 | Win32 API (`CreateFile`/`ReadFile`/`WriteFile`) | 零依赖 |
| 线程模型 | 主线程(UI) + 串口 Overlapped I/O | `FILE_FLAG_OVERLAPPED` 异步读 |
| OLED 预览 | GDI (`CreateCompatibleDC`/`StretchBlt`/`SetPixel`) | 本地软件模拟 |
| 链接库 | `comctl32.lib` `setupapi.lib` | Common Controls + 串口枚举 |
| 中文字符 | `L"..."` 宽字面量 + UTF-8 BOM 源文件 | MSVC 正确识别 |

### 2.2 项目文件结构

```
pc_host/
├── oled_control.sln
└── oled_control/
    ├── oled_control.vcxproj          # Win32Proj, v142, C++17, Unicode
    ├── oled_control.vcxproj.filters
    ├── oled_control.rc               # 对话框资源 (UTF-8 BOM)
    ├── resource.h                     # 控件 ID 定义
    ├── res/oled_control.ico
    ├── protocol.h / .cpp             # 二进制帧协议 (CRC-8-ATM, 状态机)
    ├── serial_port.h / .cpp          # Win32 串口驱动
    ├── frame_queue.h                  # 线程安全环形队列 + TxTask
    ├── oled_preview.h / .cpp         # GDI OLED 预览控件 (WndProc)
    ├── oled_preview_gen.h            # 字模数据 (316 汉字 + ASCII + 天气图标)
    └── main.cpp                       # WinMain + DialogProc
```

### 2.3 主界面布局

```
┌─────────────────────────────────────────────────────────┐
│  OLED 控制中心                              COM3 ● 已连接 │
├────────────────────┬────────────────────────────────────┤
│  串口设置            │                                    │
│  端口: [COM3 ▼]      │    ┌────────────────────────────┐  │
│  [打开] [关闭]       │    │    OLED 模拟预览             │  │
│                      │    │    128×64 GDI 4x 放大        │  │
│  LED 控制            │    │    深灰底+亮黄绿像素          │  │
│  ● 关 ○ 开 ○ 闪烁    │    │    本地模拟全部特效          │  │
│                      │    └────────────────────────────┘  │
│  显示模式             │                                    │
│  ● 静态 ○ 左滚        │    ┌────────────────────────────┐  │
│  ○ 右滚 ○ 上滚        │    │  设备状态                    │  │
│  ○ 下滚 ○ 翻页        │    │  LED: 关   模式: 静态       │  │
│  ○ 淡入淡出           │    │  延迟: -- ms                │  │
│                      │    └────────────────────────────┘  │
│  文字内容             │                                    │
│  ┌────────────────┐ │    ┌────────────────────────────┐  │
│  │ (多行输入)      │ │    │  按键日志                    │  │
│  └────────────────┘ │    │  [HH:MM:SS] KEYx 短按       │  │
│  [发送文字]          │    │  ...(最多100条, 自动滚动)   │  │
│                      │    └────────────────────────────┘  │
│  上电默认文字         │                                    │
│  [欢迎进入系统] [保存] │                                    │
│                      │                                    │
│  天气模拟             │                                    │
│  类型:[晴▼] 温度:[25]℃│                                   │
│  湿度:[65]% 风向:[北▼] │                                   │
│  [发送天气]           │                                    │
│                      │                                    │
│  [同步时间]           │                                    │
└──────────────────────┴────────────────────────────────────┘
```

### 2.4 各模块功能规格

#### 2.4.1 串口管理
- `SerialPort::Enumerate()` 枚举 COM1~COM16，`SetupDiGetClassDevs` 获取友好名称
- 参数：115200/8N1/无流控/DTR+RTS 使能
- 读超时：`ReadIntervalTimeout=50ms`, `ReadTotalTimeoutConstant=100ms`
- 发送：同步 `WriteFile`（数据极小，不阻塞 UI）
- 接收：`FILE_FLAG_OVERLAPPED` + `CreateThread`，通过 `SerialPort::onDataReceived` 回调

#### 2.4.2 二进制帧协议
`ProtocolParser` 类与 STM32 端 `proto_feed_byte()` 完全一致：
- 7 状态状态机：`WAIT_SOF → LEN → CMD → SEQ → DATA → CRC → EOF`
- `FeedByte(uint8_t)` 逐字节喂入，返回到 true 表示帧完成
- `BuildFrame()` 构建发送帧（SOF+LEN+CMD+SEQ+DATA+CRC8+EOF）
- `CheckTimeout()` 500ms 超时自动复位状态机
- CRC8 查表与 STM32 端 `crc8_table[256]` 完全一致（多项式 0x07）

命令码：`0x01`(LED) `0x02`(模式) `0x03`(文字) `0x04`(时间) `0x05`(天气) `0x06`(默认文字) `0x10`(LED上报) `0x11`(模式上报) `0x12`(按键) `0xF0`(ACK) `0xFF`(NAK)

#### 2.4.3 OLED 模拟预览
`OledPreview` 类，独立窗口类 `"OledPreviewWnd"`：
- 128×64 内存位图 (`CreateCompatibleBitmap`) → `StretchBlt` 4x 等比放大
- 颜色：背景 `RGB(32,32,32)`，像素 `RGB(180,255,80)`
- ASCII 8×16 字库：95 字符，内嵌 `ascii_8x16[95][16]`
- 中文 16×16 字库：316 字，`oled_preview_gen.h`（从 STM32 font.c 自动提取生成）
- 7 种特效动画：静态/左滚/右滚/上滚/下滚/翻页(3s)/淡入淡出(32级)
- `Tick()` 方法由主窗口 `WM_TIMER(50ms)` 驱动

#### 2.4.4 超时重传
- `TxTask` 结构：帧数据 + 命令码 + 序号 + 发送时刻 + 重试计数
- `WM_TIMER(100ms)` 扫描 `g_pendingAcks` 队列
- 500ms 无 ACK → 重发，最多 3 次 → 放弃并记录"通信超时"

#### 2.4.5 时间同步
- 打开串口时自动调用 `SyncTime()`
- 取 `localtime_s` → 编码为 7 字节 `CMD_TIME_SYNC`：[年-2000][月][日][时][分][秒][星期]
- 可手动点击"同步时间"按钮

#### 2.4.6 天气模拟
- 天气类型：晴/多云/阴/小雨/大雨/雷雨/雪 → `CMD_WEATHER_DATA`(0x05) 4 字节
- 温度：`int8_t` -20~50℃
- 湿度：0~100%
- 风向：北/东北/东/东南/南/西南/西/西北

---

## 三、STM32F407 端需求

### 3.1 固件框架
- 裸机开发，STM32 HAL 库
- 编译器：`arm-none-eabi-gcc` + Makefile

### 3.2 外设配置
| 外设 | 引脚 | 参数 |
|------|------|------|
| USART1 (PC通信) | PA9(TX), PA10(RX) | 115200/8N1 |
| USART2 (调试) | PA2(TX), PA3(RX) | 115200/8N1, `DEBUG_UART_ENABLE` 控制 |
| I²C2 (OLED) | PB10(SCL), PB11(SDA) | Fast Mode 400kHz |
| LED | PF9 | 推挽输出 |
| KEY1~4 | PE1~PE4 | 上拉输入 |
| IWDG | — | `IWDG_ENABLE` 控制 |

### 3.3 固件变更 (v1.4 → v2.0)
| 文件 | 改动 |
|------|------|
| `user_app.c:23` | 注释 `rk3506` → `PC上位机` |
| `display_mgr.c:tick()` | 移除 `display_mgr_sync_frame()` 定时调用 |
| 其他 | 不变 |

---

## 四、通信协议

```
┌──────┬──────┬──────┬──────┬────────────┬──────┬──────┐
│ SOF  │ LEN  │ CMD  │ SEQ  │   DATA     │ CRC8 │ EOF  │
│ 0xA5 │ 1B   │ 1B   │ 1B   │  0~251B    │ 1B   │ 0x5A │
└──────┴──────┴──────┴──────┴────────────┴──────┴──────┘
```

- CRC-8-ATM: 多项式 0x07，初始值 0x00，覆盖 SOF~DATA
- PC 为主设备，STM32 收到指令回复 ACK/NAK
- STM32 主动上报按键事件 (0x12)，PC 回复 ACK
- 500ms 超时重传，最多 3 次

---

## 五、编译与运行

### 5.1 Windows 上位机
```
1. VS2019 打开 pc_host/oled_control.sln
2. 选择 Debug/Release + Win32/x64
3. 生成 → 产物 pc_host/oled_control/Debug/oled_control.exe

命令行编译：
MSBuild pc_host/oled_control.sln /t:Build /p:Configuration=Debug /p:Platform=Win32
```

### 5.2 STM32F407
```bash
cd stm32f407
make
make flash
```

### 5.3 设备连接
```
USB-TTL TX → PA10 (USART1 RX)
USB-TTL RX → PA9  (USART1 TX)
USB-TTL GND → GND

OLED SCL → PB10 | SDA → PB11 | VCC → 3.3V | GND → GND
```

---

## 六、关键注意事项

1. 所有源文件必须为 UTF-8 BOM 编码（MSVC CP936 环境下的中文兼容要求）
2. `#define _WIN32_WINNT 0x0601` 必须出现在 `<windows.h>` 之前（启用 `GetTickCount64`）
3. `windowsx.h` 提供 `ComboBox_AddString`、`Button_GetCheck` 等控件宏
4. OLED 预览不依赖下位机显存上传——取消 `CMD_FRAME_SYNC`(0x20) 的使用
5. RK3506 目录保留为预留
6. 串口枚举依赖 `setupapi.lib`

---

## 七、确认与决策记录

| 编号 | 决策项 | 结论 |
|------|--------|------|
| D1 | 上位机框架 | VS2019 + Win32 对话框（非 MFC） |
| D2 | OLED 预览 | 本地 GDI 软件模拟 |
| D3 | RK3506 | 目录保留为预留 |
| D4 | 通信协议 | 沿用 v1.4 二进制帧协议 |
| D5 | C++ 标准 | C++17 |
| D6 | 编码方案 | UTF-8 BOM |
| D7 | 串口库 | Win32 API（零依赖） |
| D8 | 文件头 | `_WIN32_WINNT=0x0601` + `windowsx.h` |

---

**文档版本**: v2.0
**创建日期**: 2026-07-29
**最后更新**: 2026-08-03
**状态**: 已实现，编译通过
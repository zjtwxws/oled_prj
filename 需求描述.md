## 项目需求描述 (v2.0)

1. 创建 Windows 串口上位机（VS2019 + Win32 API + C++17），通过 USB 转 TTL 串口直连 STM32F407 开发板（USART1: PA9/PA10, 115200/8N1），控制 407 上连接的 0.96 寸 OLED 屏 (128×64, SSD1306, I²C)。
2. 上位机采用纯 Win32 对话框应用（无 MFC 依赖，`<Keyword>Win32Proj</Keyword>`），广告屏控制中心风格，功能包括：串口连接管理、LED 开关/闪烁控制、7 种显示模式切换、实时文字下发、天气模拟、PC 系统时间同步、上电默认文字 Flash 保存。
3. 上位机内置 OLED 本地 GDI 软件模拟预览（128×64 像素 4x 放大，含滚动/翻页/淡入淡出动画），不依赖下位机显存上传。
4. 通信协议沿用自定义二进制帧协议（SOF 0xA5 + LEN + CMD + SEQ + DATA + CRC-8-ATM + EOF 0x5A），与 STM32 端 protocol.c 完全一致。超时重传：500ms × 3 次。
5. RK3506 目录保留为预留，当前阶段不使用。
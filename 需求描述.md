## 项目需求描述 (v3.1)

> v3.1 核心变更：增加本地/远程双模式。本地模式 STM32 自行渲染文字+特效（无需 PC）；远程模式 PC 全控渲染帧缓冲下发，支持时间/天气/日期等模式。集成板载多级菜单系统。

1. 创建 Windows 串口上位机（VS2019 + Win32 API + C++17），通过 USB 转 TTL 串口直连 STM32F407 开发板（USART1: PA9/PA10, 115200/8N1），控制 407 上连接的 0.96 寸 OLED 屏 (128×64, SSD1306, I²C)。
2. 上位机采用纯 Win32 对话框应用（无 MFC 依赖），广告屏控制中心风格，功能包括：串口连接管理、LED 开关/闪烁控制、本地/远程模式切换、远程模式下显示模式切换（文字/时间时钟/天气/日期）及帧缓冲渲染下发、上电默认文字 Flash 保存。
3. 支持**本地模式**与**远程模式**：
   - **远程模式**：上位机内置 OLED 本地 GDI 软件模拟预览（128×64 像素 4x 放大），PC 端全屏渲染 128×64 帧缓冲后通过协议发送给 STM32，STM32 仅接收帧缓冲并写入 SSD1306。所有显示模式及特效（滚动/翻页/淡入淡出等）均在 PC 端完成。
   - **本地模式**：断开 PC 后 STM32 独立工作，自主渲染文字内容及 7 种显示特效（静态/左滚/右滚/上滚/下滚/翻页/淡入淡出），不需要状态栏，OLED 不显示 LED 信息。
   - 模式切换：支持上位机协议命令切换和板载按键切换两种方式。
4. STM32 端集成**多级菜单系统**（menu_mgr），8 项主菜单：工作模式（本地/远程）、显示内容（时间/天气/日期/自定义文字）、显示特效（7 种）、设置（对比度）、LED 控制、上电文字、系统信息、预留（三级示例）。通过 4 个板载按键导航，支持 VALUE 数值编辑和 TOGGLE 开关切换。
5. 通信协议沿用自定义二进制帧协议（SOF 0xA5 + LEN + CMD + SEQ + DATA + CRC-8-ATM + EOF 0x5A），与 STM32 端 protocol.c 完全一致。超时重传：500ms × 3 次。远程模式下帧缓冲传输使用 CMD_FRAME_SYNC 分段下发。
6. RK3506 目录保留为预留，已实现基础网关代码（mongoose HTTP/WebSocket + UART 转发），当前阶段不使用。

## 实现状态

| 功能 | 状态 |
|------|------|
| Windows 上位机 (VS2019 Win32 对话框) | ✓ 已完成 |
| GDI OLED 模拟预览 (本地/远程双模式) | ✓ 已完成 |
| 串口管理 (枚举/打开/收发/Overlapped) | ✓ 已完成 |
| 二进制帧协议 (编解码/CRC8/状态机) | ✓ 已完成 |
| 本地模式文字+7种特效 | ✓ 已完成 |
| 远程模式帧缓冲分段下发 | ✓ 已完成 |
| 远程显示模式 (文字/时间/天气/日期) | ✓ 已完成 |
| LED 控制 | ✓ 已完成 |
| 上电默认文字 Flash 保存 | ✓ 已完成 |
| 多级菜单系统 (menu_mgr) | ✓ 已完成 |
| 按键长按快速滚动 | ✓ 已完成 |
| 模式切换 (上位机命令+板载按键) | ✓ 已完成 |
| 串口断开自动检测 | ✓ 已完成 |
| 超时重传 (500ms×3) | ✓ 已完成 |
| RK3506 网关 (预留) | ✓ 基础代码已实现 |
